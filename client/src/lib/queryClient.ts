// QueryClient removed - React Query dependency was removed from project
// This file is no longer needed as the Snake game doesn't use server-side queries

// Placeholder to prevent import errors - this file should be removed when no longer referenced
export const queryClient = null;
